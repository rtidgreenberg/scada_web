[pytest]
# Markers
markers =
    pipeline: tests requiring the full sim → selector → scada_web pipeline

# Default: run all tests including pipeline tests
addopts = --tb=short -q

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

fastapi>=0.100
uvicorn>=0.20
pyyaml>=6.0
# WebSocket protocol implementation for uvicorn. Without it uvicorn rejects the
# upgrade request and the WIS /dds/websocket/{conn} surface cannot serve clients.
websockets>=10.0
